name: untitled
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.4 #supabase라이브러리
  image_picker: ^1.1.2 #이미지 데이터 가져오기
#  flutter_naver_map: ^1.2.0 #네이버 지도
  geolocator: ^13.0.3 #지오코딩(주소-> 위경도 변환)
  http: ^1.3.0 #http 요청 통신 라이브러리
  daum_postcode_search: ^0.0.3 #다음 주소검색
  flutter_inappwebview: ^6.1.5 #다음주소검색 화면 UI 구성
  intl: ^0.20.2 #data format활용


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/
  uses-material-design: true
